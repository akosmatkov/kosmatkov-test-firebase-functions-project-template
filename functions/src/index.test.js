import {describe, it} from "mocha";
import {assert} from "chai";

describe("Cookie monster wants tests.", function () {
  it("should be a test", function () {
    assert.fail("Unimplemented.");
  });
});
